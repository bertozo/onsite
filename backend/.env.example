# Local development defaults matching docker-compose.yml.
DB_URL=jdbc:postgresql://localhost:5432/onsite
DB_USER=onsite
DB_PASSWORD=onsite_dev_only

# Supabase project settings (Settings > API > JWT Settings in the Supabase dashboard).
# Until a real Supabase project exists, use tools/make_dev_jwt.py with this same secret
# to mint test tokens shaped like the ones Supabase Auth issues.
SUPABASE_JWT_SECRET=dev-only-secret-change-me
SUPABASE_JWT_ISSUER=supabase

PORT=8080
